<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me">
<select id="login" resultType="member">
	SELECT * FROM PROJECT_MEMBER WHERE GUARDIAN_ID = #{guardian_id}
</select>
	<select id="list" resultType="que">
	 	select  * from notice
	</select>
	<select id="guardian" resultType="member">
		SELECT * FROM member WHERE member_id = #{guardian_id}
	</select>
	<insert id="insertAlarm">
		insert into
	</insert>
<select id="idcheck" resultType="String">
	SELECT COUNT(*) FROM PROJECT_MEMBER WHERE GUARDIAN_ID = #{guardian_id}
</select>
<insert id="join">
	INSERT INTO project_member (GUARDIAN_ID, GUARDIAN_PW, GUARDIAN_EMAIL, GUARDIAN_PHONE, SOCIAL, ALRAM, PATIENT_ID, GUARDIAN_NAME) 
	VALUES (#{guardian_id}, #{guardian_pw}, #{guardian_email}, #{guardian_phone}, #{social}, #{patient_id}, #{patient_id}, #{guardian_name})
</insert>

<select id="findid" resultType="String">
	SELECT GUARDIAN_ID FROM PROJECT_MEMBER WHERE GUARDIAN_NAME = #{guardian_name} AND GUARDIAN_EMAIL = #{guardian_email} AND GUARDIAN_PHONE = #{guardian_phone}
</select>

<update id="resetpw">
	UPDATE GUARDIAN_ID	SET GUARDIAN_PW = #{GUARDIAN_PW} WHERE GUARDIAN_ID = #{GUARDIAN_ID}
</update>
<select id= "socialCheck" resultType="String">
	SELECT COUNT(*) FROM PROJECT_MEMBER WHERE SOCIAL = #{social}
</select>
<select id="socialLogin" resultType="member">
	SELECT * FROM PROJECT_MEMBER WHERE SOCIAL = #{social}
</select>
</mapper>